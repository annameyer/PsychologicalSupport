@model IEnumerable<PsychologicalSupports.Models.Self_esteem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Indicator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.FIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Indicator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.FIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Self_esteemID, id2 = item.StudentID }) |
            @Html.ActionLink("Details", "Details", new { id = item.Self_esteemID, id2 = item.StudentID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Self_esteemID, id2 = item.StudentID })
        </td>
    </tr>
}

</table>
